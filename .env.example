# Discord Bot Token
DISCORD_TOKEN=your_discord_token_here

# Server Configuration
PORT=3000
SERVER_URL=https://your-render-app.onrender.com

# Proxy Configuration (for maintaining consistent IP)
USE_PROXY=true
PROXY_HOST=localhost
PROXY_PORT=1080
SOCKS_PORT=1080

# Note: When deploying to Render, you'll need TWO services:
# 1. Proxy Server (proxy-server.js) - This maintains the static IP
# 2. Bot Server (index.js) - Your Discord bot that connects through the proxy
#
# The proxy server should be deployed first and get a stable URL.
# Then configure the bot to connect through that proxy.
